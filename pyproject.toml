[project]
name = "foerdermittel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Coco", email = "coco@mindatlas.dev" }
]
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
]

[project.scripts]
foerdermittel = "foerdermittel:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
